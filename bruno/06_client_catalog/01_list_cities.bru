meta {
  name: Client — List active cities
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/client/cities
  body: none
  auth: none
}

headers {
  Accept: application/json
}

docs {
  # Client — Active cities

  Public endpoint. Returns active cities sorted by name.
}
