meta {
  name: Client — List categories
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/client/categories
  body: none
  auth: none
}

headers {
  Accept: application/json
}

docs {
  # Client — Active categories (tree)

  Public endpoint. Returns active root categories with their active children.
}
