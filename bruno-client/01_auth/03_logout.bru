meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/client/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{client_token}}
}

headers {
  Accept: application/json
}

docs {
  # Logout

  Revokes the current Sanctum token.

  **Response:** `204 No Content`
}
